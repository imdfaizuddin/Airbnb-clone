<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Airbnb</title>
</head>
<body>
    <form method="POST" action="/listings/<%= listing._id%>?_method=PUT">
        <h3>Edit Listing</h3>
        <label for="title">Title: </label>
        <input name="listing[title]" id="title" type="text" value="<%= listing.title %>">
        <br>
        <label for="description">Description: </label>
        <textarea name="listing[description]" id="description" cols="30" rows="10" ><%= listing.description %></textarea>
        <br>
        <label for="image">Image: </label>
        <input name="listing[image][url]" id="image" type="text" value="<%= listing.image.url %>">
        <br>
        <label for="price">Price: </label>
        <input name="listing[price]" id="price" type="text" value="<%= listing.price %>">
        <br>
        <label for="country">country:</label> 
        <input name="listing[country]" id="country" type="text" value="<%= listing.country %>">
        <br>
        <label for="location">Location: </label>
        <input name="listing[location]" id="location" type="text" value="<%= listing.location %>">
        <button type="submit">Submit</button>
    </form>
</body>
</html>